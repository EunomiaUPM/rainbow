openapi: 3.1.0
info:
  title: IDSA Transfer Process Protocol
  description: IDSA Transfer Process Protocol
  version: 1.0.0
servers:
  - url: 'https'
paths:
  /bla:
    get:
      description: New endpoint
      responses:
        200:
          description: New response
          content:
            application/json:
              schema:
                type: object
                properties:
                  bla:
                    type: string
                    example: 'bla'
  
