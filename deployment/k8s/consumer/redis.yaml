# Consumer Redis - Deployment, Service, Secret
---
apiVersion: v1
kind: Secret
metadata:
  name: consumer-redis-secret
  namespace: rainbow-consumer
type: Opaque
stringData:
  REDIS_PASSWORD: ds_core_consumer_redis

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer-redis
  namespace: rainbow-consumer
  labels:
    app: consumer-redis
    stack: consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumer-redis
  template:
    metadata:
      labels:
        app: consumer-redis
        stack: consumer
    spec:
      containers:
        - name: redis
          image: redis:alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          command:
            - redis-server
            - --requirepass
            - $(REDIS_PASSWORD)
            - --user
            - admin
            - "on"
            - ">$(REDIS_PASSWORD)"
            - "~*"
            - "+@all"
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: consumer-redis-secret
                  key: REDIS_PASSWORD
          readinessProbe:
            exec:
              command:
                - redis-cli
                - -a
                - $(REDIS_PASSWORD)
                - ping
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - redis-cli
                - -a
                - $(REDIS_PASSWORD)
                - ping
            initialDelaySeconds: 15
            periodSeconds: 20

---
apiVersion: v1
kind: Service
metadata:
  name: consumer-redis
  namespace: rainbow-consumer
  labels:
    app: consumer-redis
    stack: consumer
spec:
  type: NodePort
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 31380
      name: redis
  selector:
    app: consumer-redis
