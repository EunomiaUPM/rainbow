# Provider PostgreSQL - Deployment, Service, PVC, Secret
---
apiVersion: v1
kind: Secret
metadata:
  name: provider-postgres-secret
  namespace: rainbow-provider
type: Opaque
stringData:
  POSTGRES_USER: ds_provider
  POSTGRES_PASSWORD: ds_provider
  POSTGRES_DB: ds_provider

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: provider-postgres-pvc
  namespace: rainbow-provider
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: provider-postgres
  namespace: rainbow-provider
  labels:
    app: provider-postgres
    stack: provider
spec:
  replicas: 1
  selector:
    matchLabels:
      app: provider-postgres
  template:
    metadata:
      labels:
        app: provider-postgres
        stack: provider
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          envFrom:
            - secretRef:
                name: provider-postgres-secret
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - ds_provider
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - ds_provider
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: provider-postgres-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: provider-postgres
  namespace: rainbow-provider
  labels:
    app: provider-postgres
    stack: provider
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 31400
      name: postgres
  selector:
    app: provider-postgres
