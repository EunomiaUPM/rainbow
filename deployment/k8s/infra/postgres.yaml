# Authority/Heimdall PostgreSQL - Deployment, Service, PVC, Secret
---
apiVersion: v1
kind: Secret
metadata:
  name: authority-postgres-secret
  namespace: rainbow-infra
type: Opaque
stringData:
  POSTGRES_USER: ds_authority
  POSTGRES_PASSWORD: ds_authority
  POSTGRES_DB: ds_authority

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authority-postgres-pvc
  namespace: rainbow-infra
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: authority-postgres
  namespace: rainbow-infra
  labels:
    app: authority-postgres
    stack: infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authority-postgres
  template:
    metadata:
      labels:
        app: authority-postgres
        stack: infra
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          envFrom:
            - secretRef:
                name: authority-postgres-secret
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - ds_authority
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - ds_authority
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: authority-postgres-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: authority-postgres
  namespace: rainbow-infra
  labels:
    app: authority-postgres
    stack: infra
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 31450
      name: postgres
  selector:
    app: authority-postgres
