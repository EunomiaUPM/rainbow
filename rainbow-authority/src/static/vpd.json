{
  "id": "{{ID}}",
  "input_descriptors": [
    {
      "id": "{{CREDENTIAL_TYPE}}",
      "format": {
        "jwt_vc_json": {
          "alg": [
            "RSA"
          ]
        }
      },
      "constraints": {
        "fields": [
          {
            "path": [
                "$.vc.type"
            ],
            "filter": {
              "type": "string",
              "pattern": "{{CREDENTIAL_TYPE}}"
            }
          }
        ]
      }
    }
  ]
}